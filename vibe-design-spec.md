# Vibe Design Spec

> Output ของ Stage 2 — 2 variants ให้ลูกค้าเลือก

**Project:** D Deal Inspector & Interior
**Date:** 2026-09-21
**Scope:** หน้าแรก (mobile-first) เป็นตัวแทนของทั้งระบบ — variant ที่เลือกจะขยายไปอีก 6 หน้าที่ Stage 3

---

## Section 1 — โจทย์ที่ variant ต้องแก้

| # | โจทย์ | ที่มา |
|---|---|---|
| 1 | Inspection นำ, Interior ตาม แต่ต้องไม่ดูเป็น 2 บริษัท | brief |
| 2 | Mobile-first จริง ๆ — ทราฟฟิกมาจาก IG/TikTok | brief |
| 3 | FAB โทร/ไลน์ ค้างตลอด | brief |
| 4 | แปลง claim "ปลอดภัยแน่นอน" เป็นสิ่งที่พิสูจน์ได้ | Stage 1 — COE เปิดให้เช็กใบอนุญาตสาธารณะ |
| 5 | ต้องโชว์ราคา | Stage 1 — คู่แข่ง 10/10 โชว์หมด |
| 6 | 12 บริการต้องไม่ทำให้ IA รก | Stage 0 risk #4 |
| 7 | ต้องทำงานได้แม้ภาพผลงานมีน้อย | Stage 0 risk #1 |

---

## Section 2 — สองทางเลือก

### Variant A — “หลักฐานนำ” (Proof-First)
`design/variant-a-proof-first.html`

**แนวคิด:** ลูกค้ากลัวโดนหลอก → ยัดหลักฐานให้เร็วที่สุด ให้ตัดสินใจด้วยข้อมูล
**โครงหน้า:** Hero (ราคาอยู่ครึ่งบนทันที) → แถบตัวเลข + กล่องท้าเช็กใบอนุญาต → บริการตรวจ 3 อัน → อุปกรณ์ → **ตารางราคาเต็ม** → รายงานตัวอย่าง → บล็อก Interior → บทความ → ติดต่อ
**บุคลิก:** เย็น เนี้ยบ เป็นระบบ คล้ายเอกสารวิศวกรรมที่อ่านง่าย
**ฟอนต์:** IBM Plex Sans Thai + Sarabun
**พื้นหลัง:** `#F8FAFC` เย็น / สลับขาว

**เหมาะเมื่อ:** ลูกค้าเทียบหลายเจ้าและตัดสินด้วยราคา+ความน่าเชื่อถือ
**จุดแข็ง:** ตอบ 3 คำถามแรกของผู้ใช้ (ราคา? เชื่อได้ไหม? ตรวจอะไร?) ได้เร็วมาก · ทำงานได้ดีแม้ **ไม่มีภาพผลงานเลย**
**จุดอ่อน:** Interior ถูกลดบทบาทเป็นบล็อกเดียว · หน้าตาใกล้เคียงคู่แข่งมากกว่า · ถ้าลูกค้าอยากดันงานตกแต่ง variant นี้ให้พื้นที่น้อย

---

### Variant B — “เล่าเส้นทาง” (Journey-Led)
`design/variant-b-journey-led.html`

**แนวคิด:** ขายเส้นทางทั้งเส้น ไม่ใช่ขายบริการเป็นชิ้น — ทำให้ Interior เป็นบทสรุปที่เป็นธรรมชาติ ไม่ใช่การขายพ่วง
**โครงหน้า:** Hero เต็มจอเริ่มจากช่วงเวลาของลูกค้า ("อีก 7 วันคุณจะเซ็นรับบ้าน") → **ไทม์ไลน์ 4 ขั้น ตรวจ→รายงาน→ตรวจซ้ำ→ตกแต่ง** → กล่องใบอนุญาตเต็มหน้าจอ → บริการตรวจ 3 อัน → **Interior แบบ bento grid ภาพใหญ่** → บทความ → ติดต่อ
**บุคลิก:** อุ่น เป็นบทความ มีอารมณ์ร่วม
**ฟอนต์:** Anuphan + Noto Sans Thai
**พื้นหลัง:** `#FAF7F4` อุ่น (โทนทราย)

**เหมาะเมื่อ:** อยากให้ Interior โตและเห็น funnel ชัด
**จุดแข็ง:** ขั้นที่ 4 ของไทม์ไลน์ทำให้การขายต่อ Interior ดูสมเหตุสมผล ไม่เหมือนฉวยโอกาส · แตกต่างจากคู่แข่งชัดเจน · Interior ได้พื้นที่ภาพเยอะ
**จุดอ่อน:** **ต้องพึ่งภาพจริงคุณภาพดี** ถ้าไม่มีจะพังทันที · ราคาอยู่ลึกกว่า (อยู่ในการ์ดบริการ ไม่ใช่ hero) · ต้องมีคอนเทนต์/ภาพมากกว่าจึงจะดูเต็ม

---

## Section 3 — สิ่งที่เหมือนกันทั้ง 2 variant (ตัดสินแล้ว ไม่ต้องเลือก)

| เรื่อง | ข้อสรุป |
|---|---|
| **FAB มือถือ** | แถบเต็มความกว้างด้านล่าง ไม่ใช่ปุ่มกลมลอย — โซนนิ้วโป้ง, กดพลาดยาก, ใส่ข้อความได้ ไลน์กินพื้นที่มากกว่าโทรเพราะเป็นช่องทางหลัก |
| **FAB เดสก์ท็อป** | ปุ่มกลมซ้อนมุมขวาล่าง |
| **กัน FAB บังเนื้อหา** | `<div class="h-24 lg:h-0">` ท้าย footer + `env(safe-area-inset-bottom)` สำหรับ iPhone |
| **Touch target** | ≥ 44px ทุกจุด, ปุ่มหลัก 52–56px |
| **สีเขียว LINE** | `#06C755` สีทางการ ห้ามเปลี่ยน — ผู้ใช้ไทยจำสีนี้ได้ |
| **ลิงก์ตรวจใบอนุญาต** | ทั้ง 2 variant ลิงก์ออก `service.coe.or.th` |
| **เมนูมือถือ 2 ชั้น** | ชั้นแรก ตรวจสอบ/ตกแต่ง → ชั้นสอง รายการบริการ |
| **Interior 9 บริการ** | หน้าเดียว anchor sections ไม่แตก 9 หน้า |
| **Motion** | 150–300ms, respect `prefers-reduced-motion` |
| **Base font** | 16px, line-height 1.65–1.7 (ไทยต้องการมากกว่าละติน เพราะสระบน-ล่าง) |

---

## Section 4 — Design tokens (ทั้ง 2 variant ใช้โครงเดียวกัน)

```css
/* --- shared --- */
--accent-inspect:  #0369A1;  /* CTA + badge ฝั่งตรวจบ้าน */
--accent-interior: #D97706;  /* A */  /* #B45309 ใน B (เข้มกว่าเพื่อ contrast บนพื้นทราย) */
--line-green:      #06C755;
--destructive:     #DC2626;
--success:         #15803D;

/* --- Variant A (เย็น) --- */
--ink: #0F172A;  --ink2: #334155;
--bg: #F8FAFC;   --border: #E2E8F0;
font-head: "IBM Plex Sans Thai";  font-body: "Sarabun";

/* --- Variant B (อุ่น) --- */
--ink: #1C1917;  --ink2: #57534E;
--bg: #FAF7F4;   --bg2: #F2ECE6;  --border: #E7E0D9;
font-head: "Anuphan";  font-body: "Noto Sans Thai";
```

**หมายเหตุสำคัญ:** ฟอนต์ที่ ui-ux-pro-max แนะนำตอนแรก (Lexend + Source Sans 3) **ใช้ไม่ได้ ไม่มี subset ไทย** — เลือกใหม่ทั้งคู่จากชุดที่มี `thai` subset
**ไม่ใช้ Kanit / Prompt** ทั้งที่ยอดนิยมสูงสุดในไทย เพราะเกลื่อนเว็บ SME จนไม่มีคาแรกเตอร์ — จะดูเหมือนคู่แข่งทันที

> ⚠ สีทั้งหมดเป็น placeholder จนกว่าจะได้โลโก้จากเพจ Facebook — ถ้าแบรนด์มีสีเดิม จะ remap ก่อนเริ่ม Stage 3

---

## Section 5 — ยังไม่ได้ทำใน prototype (จะทำที่ Stage 3)

- เมนูมือถือแบบกางจริง (ตอนนี้ปุ่ม hamburger ยังไม่เปิด)
- อีก 6 หน้า (บริการ-index, บริการย่อย ×2, บทความ-list, บทความ-detail, ติดต่อ)
- ภาพจริง — ตอนนี้เป็น placeholder ลายทแยงทั้งหมด
- Google Maps embed จริง
- MDX pipeline ของบทความ
- Metadata / OG image / structured data (LocalBusiness schema)

---

## Section 6 — ✅ Client checkpoint — ตัดสินแล้ว (2026-09-21)

**ผลการเลือก: Variant A + ไทม์ไลน์ของ Variant B**

โครงหน้าที่จะ build ที่ Stage 3:
```
Nav → Hero (ราคาอยู่ครึ่งบนทันที — จาก A)
    → แถบตัวเลข + กล่องท้าเช็กใบอนุญาต COE (จาก A)
    → ★ ไทม์ไลน์ 4 ขั้น ตรวจ→รายงาน→ตรวจซ้ำ→ตกแต่ง (ยกมาจาก B)
    → บริการตรวจ 3 อัน
    → อุปกรณ์
    → ตารางราคาเต็ม
    → รายงานตัวอย่าง
    → บล็อก Interior (ขั้นที่ 4 ของไทม์ไลน์ลิงก์ลงมาที่นี่)
    → บทความ → ติดต่อ → footer → FAB
```

**เหตุผลที่ hybrid นี้ work:** ไทม์ไลน์เป็น narrative device ที่ไม่ผูกกับโทนสี — ย้ายมาอยู่บนพื้น `#F8FAFC` เย็นของ A ได้ โดยขั้นที่ 4 ใช้ `--accent-interior` เป็นตัวเปิด funnel เหมือนเดิม และไม่ต้องพึ่งภาพผลงานเพราะไทม์ไลน์เป็น icon + text ล้วน

**ยังรอจากลูกค้า (ไม่บล็อก Stage 3 — ใช้ placeholder ไปก่อน):**
1. ~~เลือก variant~~ ✅
2. ราคาจริงของแต่ละบริการ
3. เลขใบอนุญาตวิศวกร — ถ้ามี
4. โลโก้ / สีแบรนด์
5. ภาพผลงาน — มีกี่ชุด คุณภาพประมาณไหน (**ตัวนี้ตัดสินว่า B ทำได้จริงไหม**)
